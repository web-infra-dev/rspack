interface:
  display_name: 'Rspack Allocative'
  short_description: 'Profile Rspack memory, flamegraphs, and object counts'
  default_prompt: 'Use $rspack-allocative to capture local Rspack memory snapshots, generate byte and object-count flamegraphs, and explain coverage.'

policy:
  allow_implicit_invocation: false
