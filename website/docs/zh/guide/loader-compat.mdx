import { LoaderCompatibleCardList } from '../../../components/CompatibleCard.tsx';

# Loader 兼容

该索引列出了完成 Rspack 兼容性测试的 Loader 列表。你可以在 [loader-compat](https://github.com/rspack-contrib/rspack-examples/tree/main/rspack/loader-compat/rspack.config.js) 查看相关 loader 兼容性例子。

<LoaderCompatibleCardList lang="zh"></LoaderCompatibleCardList>
