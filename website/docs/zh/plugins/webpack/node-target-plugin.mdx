import { ApiMeta } from '@components/ApiMeta.tsx';

# NodeTargetPlugin

<ApiMeta addedVersion={'0.3.3'} />

此插件用来在打包时将 Node.js 的内置模块外置掉，[`externalsPresets.node`](/config/externals#node) 底层使用了该插件。

```js
new rspack.node.NodeTargetPlugin();
```
