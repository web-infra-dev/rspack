---
description: 'Hooks of the JavascriptModulesPlugin, used to hash and rewrite the content of JavaScript chunks'
---

import ChunkType from '../../types/chunk.mdx';
import HashType from '../../types/hash.mdx';
import SourceType from '../../types/source.mdx';
import { ApiMeta } from '@components/ApiMeta';
import { Collapse, CollapsePanel } from '@components/Collapse';

# JavascriptModulesPlugin

## `chunkHash`

`SyncHook<[Chunk, Hash]>`

Called when computing the chunk hash for JavaScript chunks.

<Collapse>
  <CollapsePanel className="collapse-code-panel" header="Chunk.ts" key="Chunk">
    <ChunkType />
  </CollapsePanel>
  <CollapsePanel className="collapse-code-panel" header="Hash.ts" key="Hash">
    <HashType />
  </CollapsePanel>
</Collapse>

## `renderContent`

<ApiMeta addedVersion="2.2.2" />

`SyncWaterfallHook<[Source, RenderContext]>`

Called with the rendered content of a JavaScript chunk, before the chunk's init fragments are injected and before the chunk is written out. Taps must return a `Source`, which becomes the content the following taps and the rest of the rendering pipeline see.

Use it to wrap the whole chunk content, for example in a `try`/`catch`:

```js title="rspack.config.mjs"
class WrapChunkPlugin {
  apply(compiler) {
    const { ConcatSource } = compiler.rspack.sources;
    compiler.hooks.compilation.tap('WrapChunkPlugin', (compilation) => {
      const hooks =
        compiler.rspack.javascript.JavascriptModulesPlugin.getCompilationHooks(
          compilation,
        );
      hooks.renderContent.tap('WrapChunkPlugin', (source, { chunk }) => {
        if (chunk.name !== 'background') return source;
        return new ConcatSource('try {\n', source, '\n} catch (e) {}');
      });
    });
  }
}
```

`RenderContext` carries the `chunk` being rendered:

```ts
type RenderContext = {
  chunk: Chunk;
};
```

<Collapse>
  <CollapsePanel
    className="collapse-code-panel"
    header="Source.ts"
    key="Source"
  >
    <SourceType />
  </CollapsePanel>
  <CollapsePanel className="collapse-code-panel" header="Chunk.ts" key="Chunk">
    <ChunkType />
  </CollapsePanel>
</Collapse>
