<>

```ts
class RuntimeModule {
  source?: {
    isRaw: boolean;
    isBuffer: boolean;
    source: Buffer;
    map?: Buffer;
  };
  stage: number;
  name: string;
  identifier(): string;
  readableIdentifier(): string;
}
```

</>
