import { PluginCompatibleCardList } from '../../../components/CompatibleCard.tsx';

# Plugin compat

This index lists the plugins that have been verified by compatibility tests with Rspack.

<PluginCompatibleCardList lang="en" />
