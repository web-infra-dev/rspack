# CollectShareEntryPlugin

Internal helper used by Module Federation’s treeshake flow to collect shared module requests before generating independent share bundles. It is wired automatically by `ModuleFederationPlugin`; you normally don’t use this plugin directly.
