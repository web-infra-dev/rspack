# ShareContainerPlugin

Builds the independent share bundle that exposes a specific shared package when Module Federation treeshake is enabled. `ModuleFederationPlugin` wires this automatically; it is not intended for manual use.
