---
description: 'This plugin corresponds to Module Federation v1.0, which is the ModuleFederationPlugin in webpack'
---

import { Stability } from '@components/ApiMeta';

# ModuleFederationPluginV1

This plugin corresponds to Module Federation v1.0, which is the [ModuleFederationPlugin in webpack](https://webpack.js.org/plugins/module-federation-plugin/).

:::tip
Module Federation v1.0 is no longer being iterated. We recommend using Module Federation v1.5 or v2.0. See [Module Federation](/guide/features/module-federation).
:::

## Examples

```js title="rspack.config.mjs"
import { rspack } from '@rspack/core';

export default {
  plugins: [
    new rspack.container.ModuleFederationPluginV1({
      name: 'host',
    }),
  ],
};
```

## Options

- **Type:**

```ts
interface ModuleFederationPluginV1Options {
  name: string;
  filename?: string;
  exposes?: Exposes;
  remotes?: Remotes;
  shared?: Shared;
  runtime?: EntryRuntime;
  shareScope?: ShareScope;
  remoteType?: ExternalsType;
  library?: LibraryOptions;
  enhanced?: boolean;
}
```

The `name` option is required. For details about the shared fields, see [ModuleFederationPlugin options](/plugins/module-federation-plugin#options). Module Federation v1 does not support `implementation`, `runtimePlugins`, or `shareStrategy`.
