<%- define(ESM_MODULE_DECORATOR) %> = <%- basicFunction("module") %> {
  module = Object.create(module);
  if (!module.children) module.children = [];
  Object.defineProperty(module, 'exports', {
      enumerable: true,
      set: <%- basicFunction("") %> {
          throw new Error(<%- _assignment_error %>);
      }
  });
  return module;
};
