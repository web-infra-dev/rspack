<%- DEFINE_PROPERTY_GETTERS %> = <%- basicFunction("exports, getters, values") %> {
	var define = <%- basicFunction("defs, kind") %> {
		for(var key in defs) {
			if(<%- HAS_OWN_PROPERTY %>(defs, key) && !<%- HAS_OWN_PROPERTY %>(exports, key)) {
				<%- _define_property_code %>
			}
		}
	};
	define(getters, "get");
	define(values, "value");
};