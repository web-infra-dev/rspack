[package]
edition    = "2021"
license    = "MIT"
name       = "rspack_glob"
repository = "https://github.com/web-infra-dev/rspack"
version    = "0.1.0"

[dev-dependencies]
tempdir = "0.3"
glob = { workspace = true }
