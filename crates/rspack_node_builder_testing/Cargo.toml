[package]
description       = "rspack node builder"
edition.workspace = true
license           = "MIT"
name              = "rspack_node_builder_testing"
repository        = "https://github.com/web-infra-dev/rspack"
version           = "0.2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
rspack_node_builder        = { workspace = true }
rspack_node_builder_macros = { workspace = true }

rspack_core  = { workspace = true }
rspack_error = { workspace = true }
rspack_napi  = { workspace = true }
rspack_node  = { workspace = true }

[build-dependencies]
rspack_node_build = { workspace = true }
