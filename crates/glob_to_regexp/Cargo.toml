[package]
edition    = "2021"
license    = "MIT"
name       = "glob_to_regexp"
repository = "https://github.com/web-infra-dev/rspack"
version    = "0.1.0"

[dependencies]
regex = { workspace = true }
