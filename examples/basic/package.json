{
  "name": "rspack-basic-example",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "sideEffects": false,
  "scripts": {
    "build": "cp -r ./cjs-modules ./node_modules/@cjs-test && pnpm --dir ../.. build:cli:dev && pnpm build:app",
    "build:app": "rspack build",
    "build:quiet": "rspack build 2>/dev/null"
  },
  "dependencies": {
    "@rspack/core": "workspace:*",
    "lodash-es": "^4.17.21",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "three": "0.169.0"
  },
  "devDependencies": {
    "@rstest/core": "^0.0.3"
  }
}
