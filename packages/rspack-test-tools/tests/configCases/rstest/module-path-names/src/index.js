import { dir, file } from './bar'
import { dir1, dir2, file1, file2, metaDir1, metaFile1, typeofMetaDir, typeofMetaFile } from './foo'

console.log(
	file1,
	file2,
	metaFile1,
	dir1,
	dir2,
	metaDir1,
	typeofMetaFile,
	typeofMetaDir,
	file,
	dir
)
