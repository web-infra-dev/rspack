{
  "extends": "../../tsconfig.base.json",
  "include": [
    "src"
  ],
  "compilerOptions": {
    // TODO: Remove skipLibCheck when fix upstream type bug
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src"
  }
}