{
  "name": "rspack-diff-lodash",
  "private":true,
  "scripts": {
    "build:webpack": "webpack -o webpack-dist -c rspack.config.js",
    "build:rspack": "RSPACK=true rspack",
    "build": "npm run build:rspack && npm run build:webpack"
  }
}