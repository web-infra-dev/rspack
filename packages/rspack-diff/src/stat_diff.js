const fs = require("fs");
const path = require("path");
/**
 *
 * @param {string} rspack_stats
 * @param {string} webpack_stats
 */
function statDiff(rspack_stats, webpack_stats) {
	const rspackStats = fs.read;
}

module.exports = {
	statDiff
};
