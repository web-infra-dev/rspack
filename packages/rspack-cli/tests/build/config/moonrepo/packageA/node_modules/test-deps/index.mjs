
				import { readFileSync } from "node:fs";
        import { join } from "node:path";
				const pkgPath = new URL('./package.json', import.meta.url);
				export default JSON.parse(readFileSync(pkgPath));
				