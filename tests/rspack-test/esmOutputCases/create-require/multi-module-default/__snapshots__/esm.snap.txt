```mjs title=main.mjs
import { __webpack_require__ } from "./runtime.mjs";

import * as __rspack_external_path from "path";
import { createRequire as __rspack_createRequire } from "node:module";
const __rspack_createRequire_require = __rspack_createRequire(import.meta.url);
__webpack_require__.add({
"./dep.js"
/*!****************!*\
  !*** ./dep.js ***!
  \****************/
(module) {
module.exports = "dep";


},
"path"
/*!***********************!*\
  !*** external "path" ***!
  \***********************/
(module) {

module.exports = __rspack_external_path;


},
});
// node:module
const external_node_module_namespaceObject = __rspack_createRequire_require("node:module");
// ./only-require.js


const only_require_require = /* createRequire() */ undefined;

const required = __webpack_require__(/*! ./dep.js */ "./dep.js");

// ./with-resolve.js


const with_resolve_require = /* createRequire() */ undefined;

const resolved = /*require.resolve*/(/*! ./dep.js */ "./dep.js");

// ./with-unknown-member.js


const with_unknown_member_require = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);

const unknownMember = with_unknown_member_require.a;

// ./mixed-parsed-and-preserved.js


const mixed_parsed_and_preserved_require = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);

const mixedRequired = __webpack_require__(/*! ./dep.js */ "./dep.js");
const mixedResolved = /*require.resolve*/(/*! ./dep.js */ "./dep.js");
const mixedUnknownMember = mixed_parsed_and_preserved_require.a;

// ./with-value-escape.js


const with_value_escape_require = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
const escapedRequire = with_value_escape_require;

const escapedRequireType = typeof escapedRequire;

// ./with-non-dominating-mutation.js


const updatedRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
function updateLater() {
	updatedRequire++;
}

const iteratedRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
function iterateLater() {
	for (iteratedRequire of []) {
	}
}

const mutationUnknowns = [
	typeof updateLater,
	typeof iterateLater,
	updatedRequire.a,
	iteratedRequire.a
];

// ./with-inline-value-escape.js


function identity(value) {
	return value;
}

const inlineEscapedRequireType = typeof identity(
	(0,external_node_module_namespaceObject.createRequire)(import.meta.url)
);

// ./with-non-deferred-value-escape.js


function with_non_deferred_value_escape_identity(value) {
	return value;
}

const with_non_deferred_value_escape_require = external_node_module_namespaceObject.createRequire("<TEST_ROOT>/esmOutputCases/create-require/multi-module-default/with-non-deferred-value-escape.js");

const nonDeferredEscapedRequireType = typeof with_non_deferred_value_escape_identity(with_non_deferred_value_escape_require);
const nonDeferredUnknownMember = with_non_deferred_value_escape_require.a;

// ./with-conditional-callee.js


const with_conditional_callee_require = ( true ? external_node_module_namespaceObject.createRequire : 0)("<ROOT>/tests/rspack-test/esmOutputCases/create-require/multi-module-default/with-conditional-callee.js");

const conditionalUnknownMember = with_conditional_callee_require.a;

// ./with-use-before-declaration.js


function readUnknownMember() {
	return with_use_before_declaration_require.a;
}

function loadBuiltin() {
	return typeof (__webpack_require__(/*! path */ "path")/* .join */.join);
}

let assignedRequire;
function readAssignedUnknownMember() {
	return assignedRequire.a;
}

const with_use_before_declaration_require = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
const assignmentSourceRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
assignedRequire = assignmentSourceRequire;

const createRequireAlias = external_node_module_namespaceObject.createRequire;
function readAliasedCalleeUnknownMember() {
	return aliasedCalleeRequire.a;
}
const aliasedCalleeRequire = createRequireAlias(import.meta.url);
const handledAliasedCalleeRequire = (0, createRequireAlias)(import.meta.url);

const beforeAliasedCalleeUnknownMember =
	readAliasedCalleeUnknownMember();
const handledAliasedCalleeRequired =
	__webpack_require__(/*! ./dep.js */ "./dep.js");
const beforeAssignmentUnknownMember = readAssignedUnknownMember();
const beforeDeclarationBuiltinJoinType = loadBuiltin();
const beforeDeclarationUnknownMember = readUnknownMember();
const shadowedCalleeThrows = (() => {
	try {
		const staleRequire = createRequire('<ROOT>/tests/rspack-test/esmOutputCases/create-require/multi-module-default/with-use-before-declaration.js');
		const createRequire = () => () => undefined;
		return false;
	} catch {
		return true;
	}
})();
const preInitializationThrows = [
	(() => {
		try {
			lexicalRequire("path");
			const lexicalRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
			return false;
		} catch {
			return true;
		}
	})(),
	(() => {
		try {
			hoistedRequire("path");
			var hoistedRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
			return false;
		} catch {
			return true;
		}
	})(),
	(() => {
		try {
			localCreateRequire('<ROOT>/tests/rspack-test/esmOutputCases/create-require/multi-module-default/with-use-before-declaration.js');
			const localCreateRequire = external_node_module_namespaceObject.createRequire;
			return false;
		} catch {
			return true;
		}
	})()
];
const preInitializationAliasThrows = [
	(() => {
		try {
			var declaratorAlias = declaratorRequire,
				declaratorRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
			declaratorAlias("./declarator-alias-must-not-be-bundled.js");
			return false;
		} catch {
			return true;
		}
	})(),
	(() => {
		try {
			var assignmentAlias;
			assignmentAlias = assignmentRequire;
			var assignmentRequire = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);
			assignmentAlias("./assignment-alias-must-not-be-bundled.js");
			return false;
		} catch {
			return true;
		}
	})()
];

// ./with-ignored-uses.js


const with_ignored_uses_require = (0,external_node_module_namespaceObject.createRequire)(import.meta.url);

const ignoredRequired = with_ignored_uses_require(/* webpackIgnore: true */ "path");
const ignoredRequiredJoinType = typeof ignoredRequired.join;
const ignoredResolved = with_ignored_uses_require.resolve(
	/* webpackIgnore: true */ "path"
);
const inlineIgnoredResolved = (0,external_node_module_namespaceObject.createRequire)(import.meta.url).resolve(
	/* webpackIgnore: true */ "path"
);
const inlineIgnoredRequired = (0,external_node_module_namespaceObject.createRequire)(import.meta.url)(
	/* webpackIgnore: true */ "path"
);
const inlineIgnoredRequiredJoinType = typeof inlineIgnoredRequired.join;

// ./index.js














it("preserves created require only for unhandled usages across modules", () => {
	expect(required).toBe("dep");
	expect(resolved).toBe("./dep.js");
	expect(unknownMember).toBe(undefined);
	expect(mixedRequired).toBe("dep");
	expect(mixedResolved).toBe("./dep.js");
	expect(mixedUnknownMember).toBe(undefined);
	expect(escapedRequireType).toBe("function");
	expect(inlineEscapedRequireType).toBe("function");
	expect(nonDeferredEscapedRequireType).toBe("function");
	expect(nonDeferredUnknownMember).toBe(undefined);
	expect(conditionalUnknownMember).toBe(undefined);
	expect(beforeAliasedCalleeUnknownMember).toBe(undefined);
	expect(beforeAssignmentUnknownMember).toBe(undefined);
	expect(beforeDeclarationBuiltinJoinType).toBe("function");
	expect(beforeDeclarationUnknownMember).toBe(undefined);
	expect(handledAliasedCalleeRequired).toBe("dep");
	expect(preInitializationAliasThrows).toEqual([true, true]);
	expect(preInitializationThrows).toEqual([true, true, true]);
	expect(shadowedCalleeThrows).toBe(true);
	expect(ignoredRequiredJoinType).toBe("function");
	expect(ignoredResolved).toBe("path");
	expect(inlineIgnoredRequiredJoinType).toBe("function");
	expect(inlineIgnoredResolved).toBe("path");
	expect(mutationUnknowns).toEqual([
		"function",
		"function",
		undefined,
		undefined
	]);
});

export { beforeAliasedCalleeUnknownMember, beforeAssignmentUnknownMember, beforeDeclarationBuiltinJoinType, beforeDeclarationUnknownMember, conditionalUnknownMember, escapedRequireType, handledAliasedCalleeRequired, ignoredRequiredJoinType, ignoredResolved, inlineEscapedRequireType, inlineIgnoredRequiredJoinType, inlineIgnoredResolved, mixedRequired, mixedResolved, mixedUnknownMember, mutationUnknowns, nonDeferredEscapedRequireType, nonDeferredUnknownMember, preInitializationAliasThrows, preInitializationThrows, required, resolved, shadowedCalleeThrows, unknownMember };

```

```mjs title=runtime.mjs

var __webpack_modules__ = {};
// The module cache
var __webpack_module_cache__ = {};
// The require function
function __webpack_require__(moduleId) {
// Check if module is in cache
var cachedModule = __webpack_module_cache__[moduleId];
if (cachedModule !== undefined) {
return cachedModule.exports;
}
// Create a new module (and put it into the cache)
var module = (__webpack_module_cache__[moduleId] = {
exports: {}
});
// Execute the module function
__webpack_modules__[moduleId](module, module.exports, __webpack_require__);

// Return the exports of the module
return module.exports;
}
// expose the modules object (__webpack_modules__)
__webpack_require__.m = __webpack_modules__;

// webpack/runtime/esm_register_module
(() => {
__webpack_require__.add = function registerModules(modules) { Object.assign(__webpack_require__.m, modules) }

})();

export { __webpack_require__ };

```