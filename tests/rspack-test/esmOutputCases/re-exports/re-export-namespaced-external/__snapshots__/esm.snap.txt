```mjs title=main.mjs
import fs_0, * as __rspack_external_fs from "fs";
import { readFile as index_readFile } from "fs";

// ./index.js

// reexport named default

// reexport default








;

it('should compile and import success', async () => {
	// make sure fs usedName is Namespace
	expect(__rspack_external_fs).toBeDefined()
	const {
		fs,
		/* star re-exported */ readFileSync,
		/* renamed re-exported */ r,
		default: defaultExport,
		fsNs,
		readFile
	} = await import(/*webpackIgnore: true*/'./main.mjs')
	expect(fs).toBeDefined()
	expect(defaultExport).toBeDefined()
	expect(fsNs).toBeDefined()
	expect(readFileSync).toBeDefined()
	expect(r).toBeDefined()
	expect(readFile).toBeDefined()
})

var index_named = (/* inlined export .named */42);
var index_starExports = (/* inlined export .starExports */42);
export { __rspack_external_fs as fsNs, fs_0 as fs, index_named as named, index_readFile as r, index_readFile as readFile, index_starExports as starExports };
export * from "fs";
export default fs_0;

```