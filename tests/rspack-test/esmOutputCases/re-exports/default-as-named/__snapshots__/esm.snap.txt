```mjs title=main.mjs
// ./index.js


it('should re-export default as named through a chain', async () => {
	const mod = await import(/* webpackIgnore: true */ './main.mjs');
	expect(mod.renamedDefault).toBe(42);
	expect(mod.named).toBe('hello');
});

var index_named = (/* inlined export .named */"hello");
var index_renamedDefault = (/* inlined export ["default"] */42);
export { index_named as named, index_renamedDefault as renamedDefault };

```