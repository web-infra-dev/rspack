```mjs title=main.mjs
import { rspackRequire, definePropertyGetters, makeNamespaceObject } from "./runtime.mjs";
import * as external from "fs";

// NAMESPACE OBJECT: ./externals.js
var externals_namespaceObject = {};
makeNamespaceObject(externals_namespaceObject);
definePropertyGetters(externals_namespaceObject, { 
  "default": () => (fs),
  readFile: () => (externals_readFile) });
var externals_namespaceObject_starExports = external;
Object.keys(externals_namespaceObject_starExports).forEach(function(key) {
  if (key !== "default" && key !== "__esModule") {
    definePropertyGetters(externals_namespaceObject, { [key]: function() { return externals_namespaceObject_starExports[key]; } });
  }
});


// ./externals.js




// ./index.js





it('should have exports', async () => {
	const { externals, default: defaultExports, readFile, externalsNs } = await import(/*webpackIgnore: true*/'./main.mjs');
  expect(externals).toBeDefined();
	expect(defaultExports).toBeDefined();
	expect(readFile).toBeDefined();
	expect(externalsNs).toBeDefined();
});

export { externals_namespaceObject as externalsNs };
export * from "fs";
export { default, default as externals, readFile } from "fs";

```

```mjs title=runtime.mjs
// The require scope
var rspackRequire = {};
export { rspackRequire };

// rspack/runtime/define_property_getters
var definePropertyGetters = (exports, getters, values) => {
	var define = (defs, kind) => {
		for(var key in defs) {
			if(hasOwnProperty(defs, key) && !hasOwnProperty(exports, key)) {
				Object.defineProperty(exports, key, { enumerable: true, [kind]: defs[key] });
			}
		}
	};
	define(getters, "get");
	define(values, "value");
};;
export { definePropertyGetters };
// rspack/runtime/has_own_property
var hasOwnProperty = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop));
// rspack/runtime/make_namespace_object
// define __esModule on exports
var makeNamespaceObject = (exports) => {
	if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
		Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
	}
	Object.defineProperty(exports, '__esModule', { value: true });
};;
export { makeNamespaceObject };


```