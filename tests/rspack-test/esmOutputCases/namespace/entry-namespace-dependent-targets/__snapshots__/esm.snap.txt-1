```mjs title=main.before.mjs
import { __webpack_require__ } from "./runtime.before.mjs";
// NAMESPACE OBJECT: ./first.js
var first_namespaceObject = {};
__webpack_require__.r(first_namespaceObject);
__webpack_require__.d(first_namespaceObject, { 
  value: () => (first_value) });


// ./trace.js
const trace = [];

// ./last.js


trace.push("last");
const value = "last";

// ./shared.js


trace.push("shared");
const shared_value = "shared";

// ./first.js



trace.push("first");
const first_value = `${(/* inlined export .value */"shared")}:first`;

// ./before.js




it("does not split a namespace after an earlier side-effect dependency", async () => {
  const mod = await import(/* webpackIgnore: true */ "./main.before.mjs");
  expect(mod.first.value).toBe("shared:first");
  expect(trace).toEqual(["last", "shared", "first"]);
});

export { first_namespaceObject as first };

```