export default [
	/This prevents using hashes of each other and should be avoided/,
	/This prevents using hashes of each other and should be avoided/,
	/This prevents using hashes of each other and should be avoided/,
	/This prevents using hashes of each other and should be avoided/
];
