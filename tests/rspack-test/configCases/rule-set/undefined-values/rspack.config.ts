import { defineConfig } from '@rspack/cli';

export default defineConfig({
  module: {
    rules: [
      {
        test: undefined,
        loader: undefined,
        use: undefined,
        options: undefined,
        resource: undefined,
      },
    ],
  },
});
