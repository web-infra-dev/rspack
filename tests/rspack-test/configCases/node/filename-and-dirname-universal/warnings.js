'use strict';

module.exports = [
  /"import\.meta\.dirname" is used and has been mocked/,
  /"import\.meta\.filename" is used and has been mocked/,
  /"import\.meta\.dirname" is used and has been mocked/,
  /"import\.meta\.filename" is used and has been mocked/,
  /"import\.meta\.dirname" is used and has been mocked/,
  /"import\.meta\.filename" is used and has been mocked/,
  /"import\.meta\.dirname" is used and has been mocked/,
  /"import\.meta\.filename" is used and has been mocked/,
];
