{
  "name": "@rspack-test/type-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsc --noEmit -p resolution-bundler/tsconfig.json && tsc --noEmit -p resolution-nodenext/tsconfig.json && pnpm run test:configs",
    "test:configs": "tsc -p ../rspack-test/tsconfig.configs.json"
  },
  "devDependencies": {
    "@rspack/cli": "workspace:*",
    "@rspack/core": "workspace:*",
    "typescript": "catalog:typescript7"
  }
}
