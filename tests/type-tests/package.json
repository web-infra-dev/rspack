{
  "name": "@rspack-test/type-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsc --noEmit -p resolution-bundler/tsconfig.json && tsc --noEmit -p resolution-bundler-import-meta-hot/tsconfig.json && tsc --noEmit -p resolution-nodenext/tsconfig.json"
  },
  "devDependencies": {
    "@rspack/cli": "workspace:*",
    "@rspack/core": "workspace:*"
  }
}
