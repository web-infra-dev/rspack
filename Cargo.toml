[workspace]
exclude = [
  "crates/node_binding",
] # Avoid including node binding, since feature unification will cause an linking issue. See: https://doc.rust-lang.org/cargo/reference/features.html#feature-unification
members = ["crates/*", "xtask"]

[profile.dev]
debug       = 2
incremental = true

[profile.release]
codegen-units = 1
debug         = false
incremental   = true
lto           = false
opt-level     = 3


# swc_atoms = {optional = true, version = "0.4.25", path = "../swc_atoms"}
# swc_bundler = {optional = true, version = "0.193.4", path = "../swc_bundler"}
# swc_cached = {optional = true, version = "0.3.15", path = "../swc_cached"}
# swc_common = {optional = true, version = "0.29.19", path = "../swc_common"}
# swc_css_ast = {optional = true, version = "0.128.3", path = "../swc_css_ast"}
# swc_css_codegen = {optional = true, version = "0.138.5", path = "../swc_css_codegen"}
# swc_css_minifier = {optional = true, version = "0.103.5", path = "../swc_css_minifier"}
# swc_css_modules = {optional = true, version = "0.14.5", path = "../swc_css_modules"}
# swc_css_parser = {optional = true, version = "0.137.5", path = "../swc_css_parser"}
# swc_css_prefixer = {optional = true, version = "0.139.5", path = "../swc_css_prefixer"}
# swc_css_utils = {optional = true, version = "0.125.3", path = "../swc_css_utils/"}
# swc_css_visit = {optional = true, version = "0.127.3", path = "../swc_css_visit"}
# swc_ecma_ast = {optional = true, version = "0.95.3", path = "../swc_ecma_ast"}
# swc_ecma_codegen = {optional = true, version = "0.128.4", path = "../swc_ecma_codegen"}
# swc_ecma_loader = {optional = true, version = "0.41.20", path = "../swc_ecma_loader"}
# swc_ecma_minifier = {optional = true, version = "0.160.4", path = "../swc_ecma_minifier"}
# swc_ecma_parser = {optional = true, version = "0.123.4", path = "../swc_ecma_parser"}
# swc_ecma_preset_env = {optional = true, version = "0.175.4", path = "../swc_ecma_preset_env"}
# swc_ecma_quote_macros = {optional = true, version = "0.34.4", path = "../swc_ecma_quote_macros"}
# swc_ecma_transforms_base = {optional = true, version = "0.112.4", path = "../swc_ecma_transforms_base"}
# swc_ecma_transforms_compat = {optional = true, version = "0.137.4", path = "../swc_ecma_transforms_compat"}
# swc_ecma_transforms_module = {optional = true, version = "0.154.4", path = "../swc_ecma_transforms_module"}
# swc_ecma_transforms_optimization = {optional = true, version = "0.168.4", path = "../swc_ecma_transforms_optimization"}
# swc_ecma_transforms_proposal = {optional = true, version = "0.145.4", path = "../swc_ecma_transforms_proposal"}
# swc_ecma_transforms_react = {optional = true, version = "0.156.4", path = "../swc_ecma_transforms_react"}
# swc_ecma_transforms_testing = {optional = true, version = "0.115.5", path = "../swc_ecma_transforms_testing"}
# swc_ecma_transforms_typescript = {optional = true, version = "0.160.4", path = "../swc_ecma_transforms_typescript"}
# swc_ecma_utils = {optional = true, version = "0.106.4", path = "../swc_ecma_utils"}
# swc_ecma_visit = {optional = true, version = "0.81.3", path = "../swc_ecma_visit"}
# swc_node_base = {optional = true, version = "0.5.8", path = "../swc_node_base"}
# swc_node_bundler = {optional = true, version = "0.19.6", path = "../swc_node_bundler"}
# swc_nodejs_common = {optional = true, version = "0.0.4", path = "../swc_nodejs_common"}
# swc_plugin = {optional = true, version = "0.90.0", path = "../swc_plugin"}
# swc_plugin_macro = {optional = true, version = "0.9.10", path = "../swc_plugin_macro"}
# swc_plugin_proxy = {optional = true, version = "0.23.3", path = "../swc_plugin_proxy"}
# swc_trace_macro = {optional = true, version = "0.1.2", path = "../swc_trace_macro"}
# testing = {optional = true, version = "0.31.20", path = "../testing"}
[workspace.dependencies]
swc                 = { version = "0.233.6" }
swc_atoms           = { version = "0.4.25" }
swc_common          = { version = "0.29.19" }
swc_css             = { version = "0.141.5" }
swc_css_prefixer    = { version = "0.139.5" }
swc_ecma_ast        = { version = "0.95.3" }
swc_ecma_codegen    = { version = "0.128.4" }
swc_ecma_transforms = { version = "0.199.4" }
swc_ecma_visit      = { version = "0.81.3" }
swc_html            = { version = "0.97.7" }
swc_html_minifier   = { version = "0.94.7" }

swc_ecma_lints      = { version = "0.67.4" }
swc_ecma_minifier   = { version = "0.160.4" }
swc_ecma_parser     = { version = "0.123.4" }
swc_ecma_preset_env = { version = "0.175.4" }
swc_ecma_utils      = { version = "0.106.4" }

preset_env_base = { version = "0.3.3" }
